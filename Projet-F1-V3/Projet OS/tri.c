//
// Created by antoi on 27/09/2022.
//

#include "tri.h"
#include <stdio.h>
#include <stdlib.h>

int tri(Voiture v[], size_t len ) {
    int temp = 0;








/*


    //calcul de la longueur de l'array arr

    //affichage des éléments de l'array
    printf("elements de l'array: \n");
    for (int i = 0; i < length; i++) {
        printf("%d ", arr[i]);
    }

    //trier l'array
    for (int i = 0; i < length; i++) {
        for (int j = i+1; j < length; j++) {
            if(arr[i] > arr[j]) {
                temp = arr[i];
                arr[i] = arr[j];
                arr[j] = temp;
            }
        }
    }

    printf("\n");

    //Displaying elements of array after sorting
    printf("elements de l'array tries en ordre ascendant: \n");
    for (int i = 0; i < length; i++) {

        printf("%d ", arr[i]);
        //temp = arr[i];

    }
    printf("the length of the array registered is : %d", length);*/
}